const { PrismaClient } = require('./src/generated/client'); const prisma = new PrismaClient(); async function run() { await prisma.adminSettings.update({ where: { id: 1 }, data: { seoTitle: 'StopShop - Best Stop for You', seoDescription: 'Your number one destination.', seoKeywords: 'stopshop, ecommerce' } }); console.log('Updated DB directly'); } run().catch(function(e){console.error(e)}).finally(function(){process.exit(0)});
